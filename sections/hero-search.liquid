{{ "hero.css" | asset_url | stylesheet_tag }}

{%- assign title                 = section.settings.title -%}
{%- assign message               = section.settings.message -%}
{%- assign results_label         = section.settings.results_label -%}
{%- assign button_label          = section.settings.button_label -%}
{%- assign show_bg               = section.settings.show_bg -%}
{%- assign image                 = section.settings.image -%}
{%- assign color_palette         = section.settings.color_palette -%}
{%- assign padding_top           = section.settings.padding_top -%}
{%- assign padding_bottom        = section.settings.padding_bottom -%}
{%- assign padding_top_mobile    = section.settings.padding_top_mobile -%}
{%- assign padding_bottom_mobile = section.settings.padding_bottom_mobile -%}

{%- if results_count > 0 -%}
  {{ "date-picker.css" | asset_url | stylesheet_tag }}
{%- endif -%}

{% comment %} CSS variables start {% endcomment %}
{%- capture variables -%}
  {%- case padding_top -%}
    {%- when 'small' -%}
      --padding-top: 43px;
    {%- when 'medium' -%}
      --padding-top: 65px;
    {%- when 'large' -%}
      --padding-top: 85px;
  {%- endcase -%}

  {%- case padding_bottom -%}
    {%- when 'small' -%}
      {%- if results_count > 0  -%}
        --padding-bottom: 22px;
        {%- else -%}
        --padding-bottom: 43px;
      {%- endif -%}
    {%- when 'medium' -%}
      {%- if results_count > 0  -%}
        --padding-bottom: 32px;
      {%- else -%}
        --padding-bottom: 65px;
      {%- endif -%}
    {%- when 'large' -%}
      {%- if results_count > 0  -%}
        --padding-bottom: 43px;
      {%- else -%}
        --padding-bottom: 86px;
      {%- endif -%}
  {%- endcase -%}

  {%- case padding_top_mobile -%}
    {%- when 'small' -%}
      --padding-top-mobile: 30px;
    {%- when 'medium' -%}
      --padding-top-mobile: 40px;
    {%- when 'large' -%}
      --padding-top-mobile: 50px;
  {%- endcase -%}

  {%- case padding_bottom_mobile -%}
    {%- when 'small' -%}
      {%- if results_count > 0  -%}
        --padding-bottom-mobile: 15px;
      {%- else -%}
        --padding-bottom-mobile: 35px;
      {%- endif -%}
    {%- when 'medium' -%}
      {%- if results_count > 0  -%}
        --padding-bottom-mobile: 20px;
      {%- else -%}
        --padding-bottom-mobile: 45px;
      {%- endif -%}
    {%- when 'large' -%}
      {%- if results_count > 0  -%}
        --padding-bottom-mobile: 30px;
      {%- else -%}
        --padding-bottom-mobile: 60px;
      {%- endif -%}
  {%- endcase -%}
{%- endcapture -%}
{% comment %} CSS variables end {% endcomment %}

<div class="hero__wrapper{% if color_palette != blank %} palette-{{ color_palette }}{%- endif -%}" style="{{- variables | escape -}}">
  <div class="hero__container container{% if padding_top != blank or padding_top_mobile != blank %} hero__container--padding-top{% endif %}{% if padding_bottom != blank or padding_bottom_mobile != blank %} hero__container--padding-bottom{%- endif -%}">
    <div class="hero__content">
      {%- if title != blank -%}
        <h1 class="hero__title">{{ title }}: “{{- query -}}”</h1>
      {%- else -%}
        <h1 class="hero__title">{{ page_title }}: “{{- query -}}”</h1>
      {%- endif -%}

      {%- if results_count == 0 -%}
        {%- if message != blank -%}
          <div class="hero__message">{{- message -}}</div>
        {%- endif -%}

        {%- if button_label != blank -%}
          <a class="hero__button button button--primary button--large" href="{{ routes.all_products_collection_url }}">{{- button_label -}}</a>
        {%- endif -%}
      {%- else -%}
        <div class="hero__message">
          {{ results_count }} {{ results_label }}
        </div>
      {%- endif -%}
    </div>
  </div>

  {%- if results_count > 0 -%}
    <div class="hero__container hero__container--bottom">
      <div class="hero__container container">
        <bq-date-picker data-primary-color="{{ settings.accent_background }}"></bq-date-picker>
      </div>
      <div class="hero__overlay"></div>
    </div>
  {%- endif -%}

  <div class="hero__vision">
    {%- if show_bg -%}
      <div class="hero__vision-wrapper">
        {%- if image != blank -%}
          {%- render 'image',
            image: image,
            loading: 'lazy',
            class: 'hero__image',
            size: 'xxl'
          -%}
        {%- else -%}
          {%- render 'image-background-web' -%}
        {%- endif -%}
          <div class="hero__vision-wrapper-overlay container"></div>
      </div>
    {%- endif -%}
  </div>
</div>

{%- if results_count == 0 -%}
  <script>
    const parent = document.querySelector('#main');
    const sections = [...parent.children];

    if (sections.length <= 1) parent.classList.add('growth');
  </script>
{%- endif -%}

<script>
  const setColor = () => {
    const section = document.querySelector('.hero').nextElementSibling;

    if (!section) return false;

    const target = document.querySelector('.hero__overlay'),
          children = [...section.children];

    children.forEach(child => {
      if (child.classList.contains('palette-one')) {
        target.classList.add('palette-one')
      } else if (child.classList.contains('palette-two')) {
        target.classList.add('palette-two')
      }
    })
  }

  setTimeout(setColor, 250);

</script>

{% schema %}
  {
    "name": "Hero",
    "tag": "section",
    "class": "hero",
    "description": "Display a section with a search query and the number of results found",
    "templates": ["search"],
    "settings": [
      {
        "type": "header",
        "content": "Basic settings"
      },
      {
        "type": "paragraph",
        "content": "Below you can change the content of section"
      },
      {
        "type": "text",
        "id": "title",
        "label": "Search results title",
        "default": "Search results for"
      },
      {
        "type": "image_picker",
        "id": "image",
        "label": "Background image"
      },
      {
        "type": "paragraph",
        "content": "Note: The settings below are applicable when search results have at least 1 product"
      },
      {
        "type": "text",
        "id": "results_label",
        "label": "Results label",
        "default": "results"
      },
      {
        "type": "paragraph",
        "content": "Note: The settings below are applicable when search results are empty"
      },
      {
        "type": "contentEditor",
        "id": "message",
        "label": "Message",
        "default": "No results found"
      },
      {
        "type": "text",
        "id": "button_label",
        "label": "Button label"
      },
      {
        "type": "header",
        "content": "Advanced settings"
      },
      {
        "type": "paragraph",
        "content": "Below you can change the appearance and the components' functionality of the section"
      },
      {
        "type": "select",
        "id": "color_palette",
        "label": "Color palette",
        "options": [
          {
            "value": "one",
            "label": "Color set 1"
          },
          {
            "value": "two",
            "label": "Color set 2"
          }
        ],
        "default": "two"
      },
      {
        "type": "checkbox",
        "id": "show_bg",
        "default": true,
        "label": "Show background image"
      },
      {
        "type": "header",
        "content": "Desktop settings"
      },
      {
        "type": "select",
        "id": "padding_top",
        "label": "Padding top",
        "options": [
          {
            "value": "small",
            "label": "Small"
          },
          {
            "value": "medium",
            "label": "Medium"
          },
          {
            "value": "large",
            "label": "Large"
          }
        ],
        "default": "large"
      },
      {
        "type": "select",
        "id": "padding_bottom",
        "label": "Padding bottom",
        "options": [
          {
            "value": "small",
            "label": "Small"
          },
          {
            "value": "medium",
            "label": "Medium"
          },
          {
            "value": "large",
            "label": "Large"
          }
        ],
        "default": "large"
      },
      {
        "type": "header",
        "content": "Mobile settings"
      },
      {
        "type": "select",
        "id": "padding_top_mobile",
        "label": "Padding top",
        "options": [
          {
            "value": "small",
            "label": "Small"
          },
          {
            "value": "medium",
            "label": "Medium"
          },
          {
            "value": "large",
            "label": "Large"
          }
        ],
        "default": "large"
      },
      {
        "type": "select",
        "id": "padding_bottom_mobile",
        "label": "Padding bottom",
        "options": [
          {
            "value": "small",
            "label": "Small"
          },
          {
            "value": "medium",
            "label": "Medium"
          },
          {
            "value": "large",
            "label": "Large"
          }
        ],
        "default": "large"
      }
    ]
  }
{% endschema %}
