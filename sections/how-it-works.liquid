{%- if section.blocks.size > 0 -%}

  {{ "how-it-works.css" | asset_url | stylesheet_tag }}

  {%- assign tag                   = section.settings.tag -%}
  {%- assign title                 = section.settings.title -%}
  {%- assign title_bg              = section.settings.title_bg -%}
  {%- assign description           = section.settings.description -%}
  {%- assign padding_top           = section.settings.padding_top -%}
  {%- assign padding_bottom        = section.settings.padding_bottom -%}
  {%- assign padding_top_mobile    = section.settings.padding_top_mobile -%}
  {%- assign padding_bottom_mobile = section.settings.padding_bottom_mobile -%}
  {%- assign image_placeholder     = settings.image_placeholder -%}

  {% comment %} CSS variables start {% endcomment %}
  {%- capture variables -%}
    {%- case padding_top -%}
      {%- when 'small' -%}
        --padding-top: 50px;
      {%- when 'medium' -%}
        --padding-top: 80px;
      {%- when 'large' -%}
        --padding-top: 112px;
    {%- endcase -%}

    {%- case padding_bottom -%}
      {%- when 'small' -%}
        --padding-bottom: 50px;
      {%- when 'medium' -%}
        --padding-bottom: 80px;
      {%- when 'large' -%}
        --padding-bottom: 112px;
    {%- endcase -%}

    {%- case padding_top_mobile -%}
      {%- when 'small' -%}
        --padding-top-mobile: 40px;
      {%- when 'medium' -%}
        --padding-top-mobile: 60px;
      {%- when 'large' -%}
        --padding-top-mobile: 80px;
    {%- endcase -%}

    {%- case padding_bottom_mobile -%}
      {%- when 'small' -%}
        --padding-bottom-mobile: 40px;
      {%- when 'medium' -%}
        --padding-bottom-mobile: 60px;
      {%- when 'large' -%}
        --padding-bottom-mobile: 80px;
    {%- endcase -%}
  {%- endcapture -%}
  {% comment %} CSS variables end {% endcomment %}

  <div
    class="algorithm__wrapper{% if padding_top != blank or padding_top_mobile != blank %} algorithm__wrapper--padding-top{% endif %}{% if padding_bottom != blank or padding_bottom_mobile != blank %} algorithm__wrapper--padding-bottom{%- endif -%}"
    style="{{- variables | escape -}}"
  >
    <div class="algorithm__container container">
      {%- if tag != blank or title != blank or description != blank or title_bg != blank or view_all != blank -%}
        {%- render 'section-header',
            tag: tag,
            title: title,
            description: description,
            background: title_bg
        -%}
      {%- endif -%}
    </div>

    <div class="algorithm__list">
      <div class="algorithm__container container">
        {%- for block in section.blocks -%}
          {%- assign index        = forloop.index -%}
          {%- assign heading      = block.settings.heading -%}
          {%- assign text         = block.settings.text -%}
          {%- assign button_1     = block.settings.button_1 -%}
          {%- assign button_2     = block.settings.button_2 -%}
          {%- assign button_url_1 = block.settings.button_url_1 -%}
          {%- assign button_url_2 = block.settings.button_url_2 -%}
          {%- assign show_image   = block.settings.show_image -%}
          {%- assign image        = block.settings.image -%}

          {%- if image != blank -%}
            {%- assign image = block.settings.image -%}
          {%- elsif image_placeholder != blank -%}
            {%- assign image = image_placeholder -%}
          {%- else -%}
            {%- assign image = blank -%}
          {%- endif -%}

          <div class="algorithm__item" id="{{- block.id -}}">
            <div class="algorithm__item-col">
              <div class="algorithm__text-area">
                <span class="algorithm__index">{{- index -}}</span>

                {%- if heading != blank -%}
                  <h3 class="algorithm__heading">
                      {{- heading -}}
                  </h3>
                {%- endif -%}

                {%- if text != blank -%}
                  <div class="algorithm__text">{{- text -}}</div>
                {%- endif -%}

                {%- if button_1 != blank and button_url_1 != blank or button_2 != blank and button_url_2 != blank -%}
                  <div class="algorithm__buttons">
                    {%- if button_1 != blank and button_url_1 != blank -%}
                      <a href="{{  button_url_1 }}" class="algorithm__button button button--outlined button--large">{{- button_1 -}}</a>
                    {%- endif -%}

                    {%- if button_2 != blank and button_url_2 != blank -%}
                      <a href="{{  button_url_2 }}" class="algorithm__button button button--secondary button--large">
                        {{- button_2 -}}
                        {%- render 'icon-arrow-right' -%}
                      </a>
                    {%- endif -%}
                  </div>
                {%- endif -%}
              </div>
            </div>

            {%- if show_image -%}
              <div class="algorithm__item-col{% if image == blank %} no-image{%- endif -%}">
                {%- if image != blank -%}
                  {%- render 'image',
                    image: image,
                    loading: 'lazy',
                    class: 'algorithm__image',
                    size: 'm'
                  -%}
                {%- endif -%}
              </div>
            {%- endif -%}
          </div>
        {%- endfor -%}
      </div>
    </div>
  </div>
{%- endif -%}

{% schema %}
  {
    "name": "How it works",
    "description": "Display a section of system algorithms (step instructions).",
    "tag": "section",
    "class": "algorithm",
    "settings": [
      {
        "type": "header",
        "content": "General settings"
      },
      {
        "type": "text",
        "id": "tag",
        "label": "Tag",
        "default": "Rent a product"
      },
      {
        "type": "text",
        "id": "title",
        "label": "Title",
        "default": "How does it work"
      },
      {
        "type": "text",
        "id": "title_bg",
        "label": "Title on the background",
        "default": "How does it work"
      },
      {
        "type": "contentEditor",
        "id": "description",
        "label": "Description",
        "default": "Lorem ipsum dolor sit amet, consectetur adipiscing elit."
      },
      {
        "type": "header",
        "content": "Desktop settings"
      },
      {
        "type": "select",
        "id": "padding_top",
        "label": "Padding top",
        "options": [
          {
            "value": "small",
            "label": "Small"
          },
          {
            "value": "medium",
            "label": "Medium"
          },
          {
            "value": "large",
            "label": "Large"
          }
        ],
        "default": "medium"
      },
      {
        "type": "select",
        "id": "padding_bottom",
        "label": "Padding bottom",
        "options": [
          {
            "value": "small",
            "label": "Small"
          },
          {
            "value": "medium",
            "label": "Medium"
          },
          {
            "value": "large",
            "label": "Large"
          }
        ],
        "default": "medium"
      },
      {
        "type": "header",
        "content": "Mobile settings"
      },
      {
        "type": "select",
        "id": "padding_top_mobile",
        "label": "Padding top",
        "options": [
          {
            "value": "small",
            "label": "Small"
          },
          {
            "value": "medium",
            "label": "Medium"
          },
          {
            "value": "large",
            "label": "Large"
          }
        ],
        "default": "large"
      },
      {
        "type": "select",
        "id": "padding_bottom_mobile",
        "label": "Padding bottom",
        "options": [
          {
            "value": "small",
            "label": "Small"
          },
          {
            "value": "medium",
            "label": "Medium"
          },
          {
            "value": "large",
            "label": "Large"
          }
        ],
        "default": "large"
      }
    ],
    "max_blocks": 10,
    "blocks": [
      {
        "type": "block",
        "name": "Step",
        "settings": [
          {
            "type": "text",
            "id": "heading",
            "label": "Heading",
            "default": "Medium length section heading goes here"
          },
          {
            "type": "contentEditor",
            "id": "text",
            "label": "Description",
            "default": "Lorem ipsum dolor sit amet, consectetur adipiscing elit. Suspendisse varius enim in eros elementum tristique. Duis cursus, mi quis viverra ornare, eros dolor interdum nulla, ut commodo diam libero vitae erat."
          },
          {
            "type": "paragraph",
            "content": "Button #1 settings"
          },
          {
            "type": "text",
            "id": "button_1",
            "label": "Label",
            "default": "Button"
          },
          {
            "type": "url",
            "id": "button_url_1",
            "label": "Button #1 URL"
          },
          {
            "type": "paragraph",
            "content": "Button #2 settings"
          },
          {
            "type": "text",
            "id": "button_2",
            "label": "Label",
            "default": "Button"
          },
          {
            "type": "url",
            "id": "button_url_2",
            "label": "Button #2 URL"
          },
          {
            "type": "checkbox",
            "id": "show_image",
            "default": true,
            "label": "Show image"
          },
          {
            "type": "image_picker",
            "id": "image",
            "label": "Image",
            "info": "Note: For better displaying, the picture should be square"
          }
        ]
      }
    ]
  }
{% endschema %}
